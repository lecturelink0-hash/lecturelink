# 버전을 고정한다. `>=` 만 두면 배포 시점마다 다른 버전이 설치돼, 로컬에서 통과한 조합과
# 운영에 올라간 조합이 달라진다 — 재현 불가능한 장애의 원인이다(2026-08-18 감사 P2).
# 올릴 때는 이 파일을 고치고 테스트를 다시 돌린 뒤 배포한다.
fastapi==0.141.1
uvicorn==0.52.3
python-dotenv==1.2.3
google-genai==2.18.1
