import {redirect} from 'next/navigation';import {getCurrentSession} from '@/lib/auth/session';import {AssignedLearning} from '@/components/student/AssignedLearning';import './learning.css';export default async function Page(){const s=await getCurrentSession();if(!s)redirect('/login?next=/learning');if(s.profile.accountType!=='student'&&s.role!=='admin')redirect('/professor');return <AssignedLearning/>}
