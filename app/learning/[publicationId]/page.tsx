import {redirect} from 'next/navigation';import {getCurrentSession} from '@/lib/auth/session';import {FormativeRunner} from '@/components/student/AssignedLearning';import '../learning.css';export default async function Page({params}:{params:Promise<{publicationId:string}>}){const s=await getCurrentSession();if(!s)redirect('/login');if(s.profile.accountType!=='student'&&s.role!=='admin')redirect('/professor');return <FormativeRunner publicationId={(await params).publicationId}/>}
